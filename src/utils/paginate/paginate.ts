/**
 * Objeto de información de configuración
 */
export type Paginate = {
    firstPage: number,
    lastPage: number,
    previousPage: number,
    perPage: number,
    totalPages: number,
    data: object,
}
