// Importaciones de tipos
import { AppInformation } from '../../../app/types/App';

/**
 * Objeto de información de configuración
 */
export type THomeInformation = {
  app: AppInformation;
  micros_endpoint: string;
};
